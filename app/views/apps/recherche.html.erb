 <% @users.each do |user| %>
	<% if user != current_user %>
    	<%= user.pseudo %><br />
    	<%= user.birthday %><br />
    	<%= user.sex %><br />
    	<%= link_to "Défi", user_new_challenge_path(user.id) %>
  <% end %>
<% end %>