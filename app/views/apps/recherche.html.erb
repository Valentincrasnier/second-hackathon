<div class="rech">
	<% @users.each do |user| %>
		<% if user != current_user %>
    	<%= user.pseudo %><br />
    	<%= user.get_age %> ans<br />
    	<%= user.sex %><br />
    	<%= link_to "Défi", user_new_challenge_path(user.id) %>
	  <% end %>
	<% end %>
</div>

